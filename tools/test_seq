#!/usr/bin/env sh

if [ $# != 1 ]; then
    echo "Usage: test_seq <seq>"
    exit 1
fi

echo $1 | RNAfold --noPS --MEA
echo
echo $1 | RNAfold --noPS --MEA --motif 'GAUACCAGCCGAAAGGCCCUUGGCAGC,(...((.(((....)))....))...),-9.22'
